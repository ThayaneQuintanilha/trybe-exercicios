// function testingScope(escopo) {
//   if (escopo === true) {
//     var ifScope = 'Não devo ser utilizada fora do meu escopo (if)';
//     ifScope = ifScope + ' ótimo, fui utilizada no escopo !';
//     console.log(ifScope);
//   } else {
//     var elseScope = 'Não devo ser utilizada fora meu escopo (else)';
//     console.log(elseScope);
//   }
//   console.log(ifScope + ' o que estou fazendo aqui ? :O'); // Se necessário esta linha pode ser removida.
// }

// testingScope(true);

// const testingScope = (escopo) => {
//   if (escopo === true) {
//     let ifScope = 'Não devo ser utilizada fora do meu escopo (if)';
//     ifScope = ifScope + ' ótimo, fui utilizada no escopo !';
//     console.log(ifScope);
//   } else {
//     let elseScope = 'Não devo ser utilizada fora meu escopo (else)';
//     console.log(elseScope);
//   }
//   console.log(`'O que estou fazendo aqui ? :O ${escopo}'`);
// }

// testingScope(false)

// 2 -----------

const oddsAndEvens = [13, 3, 4, 10, 7, 2];

    // Seu código aqui.
    // const sortNumbers = () => {
    //   return oddsAndEvens.sort()
    // }

      function sortNumbers() {
      
    }

    // console.log(oddsAndEvens.sort());


    console.log(`'Os números ${array} se encontram ordenados de forma crescente!'${oddsAndEvens}`); // será necessário alterar essa linha 😉
